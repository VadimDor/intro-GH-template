#!/usr/bin/env bash

set -ueo pipefail
shopt -s dotglob

# shellcheck source=SCRIPTDIR/../lib/utils.bash
source "$(dirname "${BASH_SOURCE[0]}")/../lib/utils.bash"

echo "<YOUR TOOL LC> depends on the following packages:"
<YOUR TOOL ELC>_list_deps
echo
